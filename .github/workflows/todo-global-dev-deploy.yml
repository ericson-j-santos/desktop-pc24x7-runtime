name: TODO Global DEV Deploy Cleanup

on:
  push:
    branches:
      - ops/todo-global-dev-deploy-20260923

permissions:
  contents: read

concurrency:
  group: todo-global-dev-deploy-pc24x7
  cancel-in-progress: true

jobs:
  cleanup:
    name: Cancel stale queued DEV deploy
    runs-on: ubuntu-latest
    steps:
      - name: Record safe cancellation
        run: |
          echo "Queued PC24x7 DEV deploy superseded because no eligible self-hosted runner picked it up."
          echo "No runtime mutation is performed by this workflow."
