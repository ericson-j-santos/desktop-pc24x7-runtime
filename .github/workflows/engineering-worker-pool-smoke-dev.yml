name: Engineering Worker Pool Smoke DEV

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/engineering-worker-pool-smoke-dev.yml
      - scripts/engineering_worker_pool_smoke_dev.py
      - scripts/engineering_worker_pool_auth_reconcile_dev.py
      - runtime/engineering-worker-pool-recovery.compose.yml
      - .sdd/specs/engineering-worker-pool-smoke-dev.requirements.md
      - .sdd/specs/engineering-worker-pool-auth-reconcile-dev.requirements.md
  workflow_dispatch:
    inputs:
      worker_pool_sha:
        description: Full immutable SHA from ericson-j-santos/engineering-worker-pool
        required: true
        type: string
        default: c9b3b6ca59f2ebed663e5242d68f727373d43a6f

permissions:
  contents: read

concurrency:
  group: engineering-worker-pool-smoke-dev
  cancel-in-progress: false

jobs:
  smoke:
    name: Worker Pool smoke on dedicated Desktop runtime
    runs-on: [self-hosted, Windows, X64, pc24x7, desktop-runtime, runtime-dev]
    timeout-minutes: 15
    env:
      WORKER_POOL_SHA: ${{ github.event_name == 'workflow_dispatch' && inputs.worker_pool_sha || 'c9b3b6ca59f2ebed663e5242d68f727373d43a6f' }}
      RULES_SHA: ae9b681b6cbe5c6e0c6c82b187b3245c0749118f
      TARGET_REPO: C:\dev\reqsys-v2-enterprise-real
      CORRELATION_ID: worker-pool-runtime-${{ github.run_id }}-${{ github.run_attempt }}
      EVIDENCE_REL: artifacts\engineering-worker-pool-smoke\evidence.json

    steps:
      - name: Checkout Desktop runtime exact SHA
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Checkout Engineering Worker Pool immutable SHA
        uses: actions/checkout@v4
        with:
          repository: ericson-j-santos/engineering-worker-pool
          ref: ${{ env.WORKER_POOL_SHA }}
          path: external/engineering-worker-pool
          fetch-depth: 1
          persist-credentials: false

      - name: Checkout canonical operational rules exact SHA
        uses: actions/checkout@v4
        with:
          repository: ericson-j-santos/chatgpt-operational-rules
          ref: ${{ env.RULES_SHA }}
          path: _rules
          fetch-depth: 1
          persist-credentials: false

      - name: Resolve ReqSys session anchor from GitHub API
        id: anchor
        uses: actions/github-script@v7
        with:
          script: |
            const { data } = await github.rest.repos.getBranch({
              owner: 'ericson-j-santos',
              repo: 'reqsys-v2-enterprise-real',
              branch: 'main'
            });
            core.setOutput('sha', data.commit.sha);

      - name: Bootstrap mandatory governed session
        id: session
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $launcher = Join-Path $env:GITHUB_WORKSPACE "_rules\scripts\session_launcher.py"
          $policy = Join-Path $env:GITHUB_WORKSPACE "_rules\config\command-gateway.policy.json"
          $anchorSha = "${{ steps.anchor.outputs.sha }}"
          $launcherArgs = @(
            "--policy", $policy,
            "--repo", $env:TARGET_REPO,
            "--session-prefix", "worker-pool-smoke",
            "--correlation-id", $env:CORRELATION_ID,
            "--expected-head", $anchorSha,
            "--sync-ref", "origin/main"
          )
          $raw = & python $launcher @launcherArgs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $line = @($raw)[-1]
          $data = $line | ConvertFrom-Json
          if ($data.result -ne "SESSION_LAUNCH_OK" -or $data.state_validated -ne $true) {
            throw "SESSION_LAUNCH_BLOCKED"
          }
          if ($data.head -ne $anchorSha) { throw "SESSION_HEAD_MISMATCH" }
          "target_path=$($data.target_path)" | Add-Content $env:GITHUB_OUTPUT
          "session_id=$($data.session_id)" | Add-Content $env:GITHUB_OUTPUT
          "anchor_sha=$anchorSha" | Add-Content $env:GITHUB_OUTPUT

      - name: Run Worker Pool smoke through Command Gateway
        shell: powershell
        env:
          TARGET_PATH: ${{ steps.session.outputs.target_path }}
          SESSION_ID: ${{ steps.session.outputs.session_id }}
          ANCHOR_SHA: ${{ steps.session.outputs.anchor_sha }}
        run: |
          $ErrorActionPreference = "Stop"
          $gateway = Join-Path $env:GITHUB_WORKSPACE "_rules\scripts\command_gateway.py"
          $policy = Join-Path $env:GITHUB_WORKSPACE "_rules\config\command-gateway.policy.json"
          $runtimeScript = Join-Path $env:GITHUB_WORKSPACE "scripts\engineering_worker_pool_smoke_dev.py"
          $workerRoot = Join-Path $env:GITHUB_WORKSPACE "external\engineering-worker-pool"
          $gatewayArgs = @(
            "--policy", $policy,
            "--correlation-id", $env:CORRELATION_ID,
            "run",
            "--cwd", $env:TARGET_PATH,
            "--session-id", $env:SESSION_ID,
            "--risk", "2",
            "--timeout", "180",
            "--expected-head", $env:ANCHOR_SHA,
            "--",
            "python", $runtimeScript,
            "--expected-runtime-sha", "${{ github.sha }}",
            "--expected-worker-pool-sha", $env:WORKER_POOL_SHA,
            "--worker-pool-root", $workerRoot,
            "--correlation-id", $env:CORRELATION_ID,
            "--output", $env:EVIDENCE_REL
          )
          & python $gateway @gatewayArgs
          exit $LASTEXITCODE

      - name: Upload sanitized evidence
        if: always() && steps.session.outputs.target_path != ''
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: engineering-worker-pool-smoke-${{ github.run_id }}
          path: ${{ steps.session.outputs.target_path }}\artifacts\engineering-worker-pool-smoke\*.json
          if-no-files-found: error
          retention-days: 30
